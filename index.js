/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppRoot from './src/component/root/AppRoot';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppRoot);
